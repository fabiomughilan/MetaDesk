services:
  - type: web
    name: metadesk-server
    runtime: node
    region: virginia
    plan: free
    buildCommand: bash build.sh
    startCommand: yarn start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        sync: false
      - key: HOST
        value: 0.0.0.0
